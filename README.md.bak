Microservices with Flask
============================

Composed of 3 containers:
- hello container
- name container
- concat container

## Hello container:

A simple container that returns a single "hello" response on every request

## Name container:

A simple container that returns the name mentioned as the first segment in the request URI

